# MESH Configuration
# For local sandbox testing, use these defaults:
MESH_ENDPOINT_URL=https://localhost:8700
MESH_MAILBOX_ID=X26ABC1
MESH_PASSWORD=password
MESH_VERIFY_SSL=false

# For production/integration environments:
# MESH_ENDPOINT=INT or LIVE
# MESH_CERT_FILE=/path/to/cert.pem
# MESH_KEY_FILE=/path/to/key.pem
MESH_CERT_FILE=
MESH_KEY_FILE=

# Batch Configuration
BATCH_SIZE=500

# AWS Configuration (optional for local testing)
AWS_REGION=eu-west-2
S3_BUCKET_NAME=nhs-mesh-messages-dev
DYNAMODB_TABLE_NAME=mesh-messages
